---
trigger: always_on
---

- Using docker for Backend (Python FastAPI) | Postgres | PgAdmin 
- Do NOT INSTALL libraries in the global environment or set up a local environment.
- Always exec in Docker for these 3.

- Always give commands to execute in Powershell not command prompt. 
- Take care of the commands given and the strings or commands being parsed and also the commands that are being written in one line or multiple new lines.